server:
  port: 8082

spring:
  application:
    name: coach-service

coach:
  session-service-url: ${SESSION_SERVICE_URL:http://localhost:8081}
  anthropic-api-key: ${ANTHROPIC_API_KEY:}

management:
  endpoints:
    web:
      exposure:
        include: health,info
